import React from 'react';
import FormList from './cc-components/FormList';

const Form = (props) => {
    return (
        <div className="Form__wrapper">
            <FormList />
        </div>
    );
}

export default Form;